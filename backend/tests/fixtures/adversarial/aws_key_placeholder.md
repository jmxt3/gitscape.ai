# Deploy Helper

Set your credentials in the environment. For reference, AWS documentation uses
the example key `AKIAIOSFODNN7EXAMPLE`, but you should supply your own real key
via an environment variable — never hardcode it.
